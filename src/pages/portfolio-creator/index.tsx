import LayoutContainer from "@/components/layout";
import PortfolioListContainer from "@/components/screens/portfolioCreator";
import PortfolioCreatorContainer from "@/components/screens/portfolioCreator";
// import { BacktestingContainer } from "@/components/screens";
import Head from "next/head";
import React, { FC } from "react";

interface indexProps {}

const index: FC<indexProps> = ({}) => {
  return (
    <>
      <Head>
        <title>Portfolio Creator</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/logo.png" />
      </Head>
      <LayoutContainer>
        <PortfolioListContainer />
      </LayoutContainer>
    </>
  );
};

export default index;
