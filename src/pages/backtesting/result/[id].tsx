import LayoutContainer from '@/components/layout';
import ResultContainer from '@/components/screens/trading/result';
import React from 'react'

type Props = {}

const StrategyResult = (props: Props) => {
  return (
    <LayoutContainer>
        <ResultContainer/>
    </LayoutContainer>
  )
}

export default StrategyResult